	// call nodejs service
	const r$CURRENT_SECONDS_UNIX = await(() => {
		const param1 = $1;	// todo add param description
		const param2 = $2;	// todo add param description
		return aw4(param1, param2)
	})();